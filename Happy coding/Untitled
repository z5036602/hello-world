#include <stdio.h>
#include <stdlib.h>
int main(int argc, char * argv[]) {

    char str[] = "a dog!";
    int len = strlen(str);
    str[len-1] = '\n';
    str[len] = '\0';
    printf("%s\n",str);
    return 0;
}
